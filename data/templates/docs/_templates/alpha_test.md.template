---
type: alpha_test
title: "Alpha Test — Task <placeholder: NN>: <placeholder: title>"
ticket: <placeholder: ticket id, or null>
status: draft
authority: secondary
protection: append-only
affects: []
depends_on: []
created: <placeholder: YYYY-MM-DD>
updated: <placeholder: YYYY-MM-DD>
tags: []
task_number: <placeholder: integer>
designed_by: da
sealed_date: null
---

<!-- DELETE THIS HEADER BEFORE PUBLISHING -->

# Alpha Test — Task <placeholder: NN>: <placeholder: title>

<!-- guidance: File goes in docs/claude_tasks/alpha_tests/NN_slug.md.
     Designed by the DA at Step 4 from the spec ONLY. Frozen (status: sealed) at Step 6.
     Executed by the DA at Step 8 (or the data-reviewer in strict separation-of-duties mode).
     THE DE NEVER TOUCHES THIS FILE (the alpha-test wall).
     status: draft | sealed | passed | failed.
     authority: secondary while draft — flips to ssot at sealing (the frozen test
     governs Step 8 acceptance). sealed_date stays null until Step 6.
     FREEZE RULE: once sealed, the ground-truth cases are immutable. /audit-corpus flags
     a sealed alpha test modified after its sealed_date as a High finding. -->

> **Designer:** da
> **Spec version tested:** `docs/claude_tasks/<NN>_<slug>.md` rev <placeholder: revision>
> **Status:** <placeholder: draft | sealed | passed | failed>
> **Sealed date:** <placeholder: null until Step 6>

---

## Derivation rule

Cases in this test derive ONLY from the spec — the DA must not read any implementation, branch, or work-in-progress code while designing or refreshing them. If the spec changes at Step 6, the DA refreshes the cases from the revised spec **before** sealing. After sealing, the cases are frozen.

---

## Ground truth cases

<!-- guidance: Each case must be independently verifiable against a named source of truth
     (a schema, an authoritative table/figure, a spec acceptance criterion). Expected results
     are concrete values or precisely-bounded ranges — never "looks reasonable". -->

| Case ID | Input / question | Expected result | Source of truth | Rationale |
|---------|------------------|-----------------|-----------------|-----------|
| <placeholder: AT-1> | <placeholder> | <placeholder> | <placeholder> | <placeholder> |
| <placeholder: AT-2> | <placeholder> | <placeholder> | <placeholder> | <placeholder> |

---

## Pass/fail criteria

<!-- guidance: Binary — no partial credit, no judgment calls at execution time.
     Define per-case criteria (what counts as a match: exact value, tolerance, must-contain)
     and the overall threshold (e.g. "all cases pass" or "N of M pass, with AT-1 mandatory"). -->

- **Per-case:** <placeholder: match rule per case or case group>
- **Overall:** <placeholder: threshold — e.g. all cases must pass>

---

## Seal record

<!-- guidance: Sealed at Step 6 — the PM directs, the DA performs (sets status: sealed,
     sealed_date, authority: ssot). Identify the exact spec revision this test was derived
     from. After sealing, the cases above are immutable. -->

- **Sealed by:** <placeholder>
- **Sealed on:** <placeholder: YYYY-MM-DD>
- **Spec revision:** <placeholder: git hash and/or spec §12 revision entry>

---

## Execution log (Step 8)

<!-- guidance: Executed by the DA at Step 8 — one row per case, verdict binary.
     Record actuals verbatim (values, output), not summaries. -->

| Case ID | Expected | Actual | PASS/FAIL |
|---------|----------|--------|-----------|
| <placeholder: AT-1> | <placeholder> | <placeholder> | <placeholder> |

**Overall verdict:** <placeholder: PASSED | FAILED> — <placeholder: YYYY-MM-DD>, executed by da

---

## Post-seal discoveries

New edge cases discovered after sealing become new tracker sub-issues — never retroactive mutation of the cases above. List discoveries here with the spawned ticket id.
