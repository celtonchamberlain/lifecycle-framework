{
  "name": "{{PROJECT_SLUG}}-governance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lifecycle Framework governance tooling for {{PROJECT_NAME}} (corpus validation, index, activity log).",
  "scripts": {
    "lint:frontmatter": "node scripts/validate-frontmatter.mjs docs .claude",
    "build:index": "node scripts/build-index.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=24"
  }
}
